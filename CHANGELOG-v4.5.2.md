# v4.5.2 — Knowledge Deploy Fix

- Исправлен Dockerfile: `data/knowledge_base` теперь копируется в Railway/Docker image.
- Экран базы больше не маскирует отсутствие данных зелёным статусом.
- При пустой базе машинно-зависимые подсказки явно блокируются предупреждением.
- Добавлены регрессионные тесты упаковки базы.
