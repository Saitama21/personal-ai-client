# Personal AI Client PRO v2.7.8 — iPad Upload Fix

- Явная кнопка выбора файла внутри зоны загрузки.
- `showPicker()` с резервным `input.click()` для Safari/iPadOS.
- Клик, Enter и Space открывают системный выбор файлов.
- После выбора файла мастер автоматически переходит к AI-анализу.
- Кнопка «Далее» на первом этапе требует файл и сама открывает выбор файла.
- Устранено перехватывание событий стеклянными слоями.
